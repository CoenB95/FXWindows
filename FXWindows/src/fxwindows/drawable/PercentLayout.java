package fxwindows.drawable;

import javafx.scene.canvas.GraphicsContext;

public class PercentLayout extends Container {

	@Override
	public void draw(GraphicsContext graphics) {
		// Width:
		// WidthBehavior:	FillWidth/WrapContent/ScaleContent(?).
		// BaseWidth:		max room allowed by container.
		// WidthTransform:	?
		// 
		// getWidth (width based on available room and behavior.
		
		// Height: depending on width.
		
		// Text: toggle wrap or not.
	}

}
